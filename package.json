{
  "name": "claude-memory-bridge",
  "version": "0.1.0",
  "description": "Share Claude Code session state across systems where the same projects are accessed via different absolute paths",
  "type": "module",
  "bin": {
    "claude-memory-bridge": "./index.ts"
  },
  "scripts": {
    "start": "bun run index.ts"
  }
}
